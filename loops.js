/* // for loop  
function loop(array) {
    for (let i = 0; i < array.length; i++) {
        console.log(`${array[i]}`);

    }
}

loop([1, 2, 3, 4]);



 */

/* calcalute the multiple no of n elements  */


let n = 2575;
for (let i = 0; i < 10; i++) {
    console.log(` ${i}   x   ${n} = ${i*n}`);

}